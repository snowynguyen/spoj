#include <bits/stdc++.h>
using namespace std;
const int N = 2e5 + 10;
int n, h;
int H[N];

void input() {
    cin >> n >> h;
    for (int i=1; i<=n; ++i) {
        cin>>H[i];
    }
}

int sub2() {
    
}

int main() 
{
    ios_base::sync_with_stdio(0); cin.tie(0);
    input();
    cout << sub2();
    return 0;
}